<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.tcc.reuniapp">

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.CAMERA" />
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity android:name=".TempoDesejadoActivity"></activity>
    <activity android:name=".HorariosDisponiveisActivity" />
    <activity android:name=".QRActivity" />
    <activity android:name=".DecoderActivity" />
    <activity
      android:name=".AgendaDrawerActivity"
      android:label="@string/title_activity_agenda_drawer"
      android:theme="@style/AppTheme.NoActionBar" />
    <activity android:name=".InfoActivity" />
    <activity android:name=".CompromissoActivity" />
    <activity android:name=".LoginActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>

</manifest>